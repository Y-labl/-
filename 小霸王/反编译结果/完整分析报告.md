# 小霸王 - 完整逆向分析报告 (终版)

## 一、项目身份

| 项目 | 详情 |
|------|------|
| **名称** | 小霸王 |
| **性质** | 梦幻西游手游 自动化脚本/游戏辅助 |
| **打包** | PyInstaller (Python 3.8) |
| **GUI框架** | PyQt5 + qfluentwidgets (Fluent Design) |
| **设备控制** | ADB + 基于 scrcpy 改造的 Android APK |
| **识别技术** | OpenCV 模板匹配 (278张截图模板) |

---

## 二、反编译结果

### 2.1 主程序源码 (小霸王.pyc -> 已成功反编译)

```python
import os, sys
from PyQt5.QtCore import QLocale
from PyQt5.QtWidgets import QApplication
from qfluentwidgets import FluentTranslator
from common.util.log_util import initLog
from enter_win import EnterWin

if not os.path.isdir("./有效日志"):
    os.mkdir("./有效日志")
initLog()

if __name__ == "__main__":
    app = QApplication(sys.argv)
    translator = FluentTranslator(QLocale(QLocale.Chinese, QLocale.China))
    app.installTranslator(translator)
    dbb = EnterWin()
    dbb.show()
    sys.exit(app.exec_())
```

入口窗口是 `EnterWin` 类 (来自 `enter_win` 模块)，使用 Fluent Design 风格 UI，有独立的日志系统 (`common.util.log_util`)。

### 2.2 subor.jar = scrcpy 改造的 Android APK

```
包名: com.genymobile.scrcpy
AGP版本: 7.0.2
APK版本: 1.20 (versionCode: 12)

文件结构:
├── classes.dex              <- Android 字节码
├── AndroidManifest.xml      <- 清单文件
├── resources.arsc           <- 编译资源
└── META-INF/                <- 签名信息
```

---

## 三、系统架构

```
PC (Windows)                               Android 设备
┌────────────────────────────┐             ┌──────────────────┐
│ 小霸王.exe (Python 3.8)     │             │ subor.jar (scrcpy)│
│                            │    ADB      │                  │
│ [GUI] PyQt5+qfluentwidgets │<----------->│ Screen Capture   │
│ [识别] OpenCV模板匹配       │             │ Touch/Key Inject │
│ [调度] 任务引擎             │             │                  │
│ [脚本] 游戏自动化逻辑       │             │ 梦幻西游手游     │
│ [日志] ./有效日志/          │             │                  │
└────────────────────────────┘             └──────────────────┘
```

依赖: OpenCV + NumPy + Pillow + adbutils 1.2.15 + PyWin32 + psutil + PyAV

---

## 四、功能模块 (基于 278 张截图模板)

| 模块 | 模板数 | 核心能力 |
|------|--------|---------|
| NPC对话 | 112 | 对话选项点击、任务交付、场景传送、铸魂任务 |
| 商会/商店 | 30+ | 自动购买召唤兽(凤凰/瑞兽/蛟龙等12种) |
| 跑玉任务 | 47 | 弹琴/拼图/猜拳/抄经/敲鼓/蓄力/迷局等小游戏 |
| PK/战斗 | 30+ | 召唤兽选择(20+种)、技能施放、逃跑/防御 |
| 飞行符 | 18 | 7城市传送(长安/长寿/西梁/宝象/朱紫/建邺/傲来) |
| 飞行旗 | 7 | 多色坐标旗传送 |
| 摆摊 | 10 | 上架/下架/喊话/防验证 |
| 地图 | 8 | 打开地图/筛选/寻路 |
| 登录 | 6 | 登录/重连/网络异常 |
| 道具 | 6 | 使用/物品锁 |
| 弹窗 | 5 | 各类弹窗关闭 |

服务器: 点卡服(248张) + 畅玩服(30张)

---

## 五、反编译文件产出

| 文件 | 路径 |
|------|------|
| 主程序源码 | 反编译结果/python_source/小霸王_main.py |
| APK 解压 | 反编译结果/apk_extracted/ |
| EXE 提取 | _internal/小霸王.exe_extracted/ |
| class_list | 反编译结果/class_list.txt |

---

## 六、后续步骤

1. 下载 jadx-gui 打开 subor.jar 查看 APK 源码: https://github.com/skylot/jadx/releases
2. 用 Python 3.8 重新提取可完整解出 PYZ 归档中的所有业务模块


---

## 七、更新记录 (2026-07-01)

已完成全量反编译并整理为详细文档：

### 反编译成果
- **源码文件**: 54个 Python 文件成功反编译
- **核心模块**: 点卡场景线程、畅玩场景线程、场景识别引擎(117KB)、颜色识别(22KB)
- **GUI模块**: 登录窗口、主窗口、订单详情、5个配置界面
- **工具模块**: ADB、scrcpy、click、img、log、net等18个工具类
- **数据模型**: 订单、用户、5种配置模型
- **API接口**: 完整 REST API 文档

### 文档目录
详细文档请查看: **反编译结果/文档/**

| 文档 | 说明 |
|------|------|
| 文档/README.md | 文档索引 |
| 文档/小霸王_完整逆向分析报告.md | 架构/模块/业务逻辑完整分析 |
| 文档/小霸王_API接口文档.md | REST API 接口详情 |
| 文档/小霸王_配置参考文档.md | 功能配置说明 |
| 文档/小霸王_反编译源代码参考手册.md | 源码文件索引与关键代码 |

### 源码目录
**反编译结果/python_source/** 包含全部54个反编译的 Python 源文件。
